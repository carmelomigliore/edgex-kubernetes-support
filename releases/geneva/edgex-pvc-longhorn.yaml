apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: edgex-redis-claim
  namespace: default
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
